require 'spec_helper'

describe Kitchen::Provisioner::Manhattan do
  it 'has a version number' do
    expect(Kitchen::Provisioner::Manhattan::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
